spring:
  application:
    name: selfin

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/selfin
    username: root

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 80